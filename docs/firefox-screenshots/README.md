# Firefox Screenshot Manifest

Last updated: March 16, 2026

Capture these Firefox desktop screenshots before AMO submission:

1. `listing-panel-firefox-desktop.png`
2. `popup-firefox-desktop.png`
3. `options-firefox-desktop.png`

Capture requirements:

- Use Firefox Desktop on macOS or Windows.
- Show the final Firefox build from `artifacts/staging/firefox/`.
- Keep browser chrome visible so AMO reviewers can see Firefox context.
- Use light mode for at least one screenshot.
- Make sure market compare is enabled for the listing screenshot.
